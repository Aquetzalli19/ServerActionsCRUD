version: '3.9'

services:
  next-app:
    container_name: next-app
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    restart: always
    ports:
      - 3000:3000
    environment:
      - NODE_ENV=development